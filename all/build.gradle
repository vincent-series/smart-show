apply from: "$rootDir/lib_module_config.gradle"
project.archivesBaseName = 'all'
dependencies {
    api project(':smart-toast')
    api project(':smart-topbar')
    api project(':smart-snackbar')
    api project(':smart-dialog')
}
