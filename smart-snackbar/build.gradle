apply from: "$rootDir/lib_module_config.gradle"
project.archivesBaseName = 'snackbar'
dependencies {
    api project(':bar-core')
    compileOnly "com.google.android.material:material:${dependencyLibVersion.material}"
}
