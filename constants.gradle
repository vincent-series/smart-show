ext {

    sdkVersionConfig = [
            compileSdkVersion: 30,
            minSdkVersion    : 15,
            targetSdkVersion : 30,
    ]

    dependencyLibVersion = [
            appcompat       : "1.2.0",
            material        : "1.3.0-alpha04",
            constraintlayout: "2.0.4",
    ]

}